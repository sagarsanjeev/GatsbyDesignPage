import React from 'react';
import { Layout, SEO } from 'components';

const BlogPage = () => (
  <Layout noBackground>
    <SEO title='Blog' />
    <div>Test</div>
  </Layout>
);

export default BlogPage;
